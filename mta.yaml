_schema-version: "3.2"
ID: travelprocessor
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: travelprocessor-destination-content
  type: com.sap.application.content
  requires:
  - name: travelprocessor-destination-service
    parameters:
      content-target: true
  - name: travelprocessor-repo-host
    parameters:
      service-key:
        name: travelprocessor-repo-host-key
  - name: travelprocessor-uaa
    parameters:
      service-key:
        name: travelprocessor-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: travelprocessor_html_repo_host
          ServiceInstanceName: travelprocessor-html5-service
          ServiceKeyName: travelprocessor-repo-host-key
          sap.cloud.service: travelprocessor
        - Authentication: OAuth2UserTokenExchange
          Name: travelprocessor_uaa
          ServiceInstanceName: travelprocessor-xsuaa-service
          ServiceKeyName: travelprocessor-uaa-key
          sap.cloud.service: travelprocessor
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: travelprocessor-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: travelprocessor-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - travelprocessor.zip
      name: travelprocessor
      target-path: resources/
- name: travelprocessor
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: travelprocessor-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: travelprocessor-html5-service
    service-plan: app-host
- name: travelprocessor-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: travelprocessor-destination-service
    service-plan: lite
- name: travelprocessor-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: travelprocessor-xsuaa-service
    service-plan: application
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
